/**
 * Canonical gesture thresholds and sensitivity presets live in {@link ../constants/defaults.js}.
 * This module re-exports them so any legacy import path or doc reference stays valid
 * without duplicating numbers (see EVALUATION.md).
 */
export { DEFAULT_THRESHOLDS, SENSITIVITY_PRESETS } from '../constants/defaults.js'
